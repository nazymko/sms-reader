package org.nazymko.stategy;

import org.nazymko.History;

/**
 * Created by Andrew Nazymko
 */
public class CardNumberMask implements Strategy<History> {
    @Override
    public void apply(History mutableTarget) {
        //mutableTarget.getSms().split()
    }
}
